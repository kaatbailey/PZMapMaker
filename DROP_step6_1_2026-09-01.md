# DROP NOTE — step 6, drop 1 — 2026-09-01

**What this is:** the copy commands and expected line counts for ONE delivery of
files, plus what its verification run should print. It is throwaway. Once the
files are copied and the run matches, delete it.

**It is NOT documentation.** Nothing here needs to survive the session. The
findings live in `FINDINGS_F5_2026-09-01.md`; the facts live in `STATE.md`.

Earlier drops in this session were all named `APPLY.md` and each silently
overwrote the last. Drop notes are named `DROP_<step>_<n>_<date>.md` from here
on so that cannot happen again.

---

Six files, all COMPLETE. Overwrite, do not edit.

**`java_random.hpp` CHANGED.** It carries a real bug fix (see
FINDINGS_F5 §1). Do not skip it.

    cd ~/Documents/PZMapMaker
    cp <download>/{CMakeLists.txt,java_random.hpp,gisimport.hpp,gisimport.cpp,gisraster_oracle.cpp,GisRasterOracle.java} .
    cp GisRasterOracle.java ~/Documents/PZMapCreation/src/main/java/pzformat/

## TOUCH FIRST — files from a session land ~4 hours in the future

Containers stamp UTC, this machine is EDT. Ninja fails with
`manifest 'build.ninja' still dirty` AFTER printing `Configuring done`, builds
nothing, and leaves the OLD binary in place. `CMakeLists.txt` is the one
`touch *.cpp *.hpp` misses.

    touch ~/Documents/PZMapMaker/*.cpp ~/Documents/PZMapMaker/*.hpp \
          ~/Documents/PZMapMaker/CMakeLists.txt \
          ~/Documents/PZMapCreation/src/main/java/pzformat/GisRasterOracle.java

## Verify the drop landed

    wc -l ~/Documents/PZMapMaker/{CMakeLists.txt,java_random.hpp,gisimport.hpp,gisimport.cpp,gisraster_oracle.cpp,GisRasterOracle.java}

    CMakeLists.txt        126
    java_random.hpp       104
    gisimport.hpp         165
    gisimport.cpp         186
    gisraster_oracle.cpp  260
    GisRasterOracle.java  286      total 1127

## Build and run

    cmake -S ~/Documents/PZMapMaker -B /tmp/noqt -G Ninja -DCMAKE_DISABLE_FIND_PACKAGE_Qt6=ON
    cmake --build /tmp/noqt --target pz_gisraster_oracle pz_palettes_oracle

    cd ~/Documents/PZMapCreation
    javac -d /tmp/g6 -sourcepath src/main/java src/main/java/pzformat/GisRasterOracle.java
    java -cp /tmp/g6 pzformat.GisRasterOracle /tmp/g.java.txt 5000 | tail -1
    /tmp/noqt/pz_gisraster_oracle /tmp/g.cpp.txt 5000 | tail -1
    diff /tmp/g.java.txt /tmp/g.cpp.txt | command grep '^<' | sed 's/^< //' | cut -f1 | sort | uniq -c

## Predictions

- 63,878 lines from both sides.
- **The diff is NOT empty, and that is the expected result.** Exactly two
  sections differ: `COS` 18 lines and `PROJ` 15,750. Both are the known
  `Math.cos` vs `std::cos` one-ulp disagreement at the 18 latitudes the corpus
  seeds ON PURPOSE. Every other section matches.
- If any OTHER section appears in that count — `GRID`, `CNT`, `CELL`, `EXT`,
  `FRECT`, `FPOLY`, `TLINE`, `WLINE`, `MIX`, `DEGEN`, `WW` — that is a real
  divergence and a finding.

## REGRESSION CHECK — the JavaRandom fix must not move the palettes digest

    java -cp /tmp/jo pzformat.PalettesOracle /tmp/p.java.txt 5000 | tail -1
    /tmp/noqt/pz_palettes_oracle /tmp/p.cpp.txt 5000 | tail -1
    cmp /tmp/p.java.txt /tmp/p.cpp.txt; and echo PALETTES-STILL-IDENTICAL
    md5sum /tmp/p.java.txt

Must still be **429,257 lines, md5 `d28516268cdaae203275e778b2176563`**. It was
unchanged here; confirming on GCC 16 is the point.

## WORTH RUNNING — steps 3 and 4 against the fixed JavaRandom

Those two were verified with the buggy `nextInt`. Bound analysis says they are
unaffected, but that is analysis, not measurement, and it is one command each:

    cmake --build /tmp/noqt --target pz_footprint_oracle pz_buildingplan_oracle
