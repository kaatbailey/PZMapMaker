## FINDINGS — F1 — 2026-08-31

**Status:** complete

**What was done:**
- Built a static call graph over all 65 files in `src/main/java/pzformat/`
  (`PZMapCreation` @ `0247ddc`): comments and string literals stripped, class
  names matched at word boundaries, transitive reachability computed from
  `GisCells` — the target of `Probe.java:29`.
- 24 files are reachable. 10 are the already-ported format layer. **14 units,
  6,854 lines, are the GIS port.**
- Verdicts assigned from the trace, not from STATE's editor-track inventory or
  E13's findings. Both lists were checked against it afterward; both are wrong.
- Produced `F1_GIS_PORT_INVENTORY.md`: full table, three verdicts, seven-step
  port order with a named oracle per step.

**Confirmed** (verified against the source tree):
- **SHIPS (14):** `GisCells` 864, `GisImport` 491, `BuildingPlan` 3347,
  `TilePalette` 324, `GroundRegions` 308, `FootprintSnap` 299, `MaskRule` 257,
  `TreeScatter` 210, `GroundPalette` 206, `BiomeMapWriter` 132, `TreePalette`
  120, `Json` 117, `GroundMaterial` 114, `GeoJson` 65.
- **`BuildingPlan` has zero `pzformat` dependencies** and is 49% of the port's
  lines. It can be ported in parallel with everything else, against its own
  14,680-layout self-test.
- **No Lua parsing is needed.** `WorldGenBiomes` and `WorldGenFeatures` are
  reached only by `BiomePalette`, which has zero callers. All three SURVEY.
  This was F1's biggest open cost question and it resolves the cheap way.
- **Six SHIPS units draw `java.util.Random`**, not one: `GisCells`,
  `GroundRegions`, `MaskRule`, `TreeScatter`, `GroundPalette`, `BuildingPlan`.
  The LCG is promoted to its own port step, ahead of everything that draws.
- **No map-iteration-order hazard.** `GisCells` uses `LinkedHashMap` for
  `shared`; the one `HashSet` (`doorAt`, line 670) is membership-only.
- **The schematic PNG is not on the generation path.** `GisCells:48` calls
  `GisImport.rasterise`, not `run`. `java.awt`/`ImageIO` are reached only from
  `run` (the `Probe gisimport` diagnostic). F5 ports `rasterise` dependency-free.
- **E15's premise, by inspection.** `GisImport.java:150–157` walks each ring
  calling `waterLine` between consecutive points — a perimeter trace, no fill.

**Unverified** (believed, not tested — say what would test it):
- **Byte-identical PNG for `biomemap_X_Y.png`.** Qt `QImage` may not reproduce
  Java `ImageIO`'s encoder byte-for-byte (colour type, bit depth, filter, zlib
  level). Test: encode the same pixel buffer through both and `cmp`. If it
  fails, step 7's oracle must compare decoded pixels, not file bytes.
- **That the public repo matches the local tree.** HEAD is 2026-08-21; today is
  2026-08-31. Test: `git status --short` in `~/Documents/PZMapCreation`.

**Corrections** (something in STATE.md / CHUNKS.md is wrong):
- CHUNKS F4 ports `WaterTiles` → **it is a read-only survey with zero callers
  and a `main`**; water tile selection is already in `TilePalette` → evidence:
  `grep -l WaterTiles *.java` returns only itself.
- STATE 2026-08-31 says `GisCells` writes through `Square` and `CellEditor` →
  **it does not**; it manipulates `CellData` via its own `appendTile:827` /
  `replaceTile:790`. `CellEditor`'s callers are `EditDemo` and `RoomGeometry`
  only → *consequence: generated output has no undo history, so "generate into
  the open project" is separate work from "generate to disk", and F7 must say
  which it is.*
- CHUNKS E15 says "small if `fillPolygon` is reusable — buildings already use
  it" → **no `fillPolygon` exists**; buildings go through `FootprintSnap.Rect`
  (E5, rectangles). E15 needs a scanline fill written.
- CHUNKS A2b `✅ CONFIRMED INERT` implies removal → **`writeWorldGenOverride` is
  still called at `GisCells.java:371`.** It will show up in F6's diff.
- STATE §6's Java inventory lists ~35 files; **65 exist**. Missing includes
  `BuildingPlan`, the largest file in the project, plus `GroundRegions`,
  `GroundMaterial`, `MaskRule`, `FootprintSnap`, `GeoJson`, `WaterTiles`.
- STATE's editor-track survey list and E13's findings both put `RoomLayout`,
  `RoomMinimums`, `RoomCluster`, `DoorProbe` in the survey bucket → **the trace
  agrees.** Recorded as a confirmation, not a correction. `HouseRules`,
  `HallRule`, `DitherLaw`, `HouseLayouts` join them; all have zero callers.

**Files changed:**
- created `F1_GIS_PORT_INVENTORY.md`
- created `FINDINGS_F1_2026-08-31.md`
- no source changed; F1 is an inventory

**Commands worth keeping:**
```fish
# Reachability from GisCells — the trace this chunk rests on
cd ~/Documents/PZMapCreation/src/main/java/pzformat
for c in (ls *.java | sed 's/.java//')
    printf '%-20s <- ' $c
    grep -l -e $c *.java | grep -v "^$c.java" | tr '\n' ' '
    echo
end

# Is the public repo current?
cd ~/Documents/PZMapCreation; and git status --short

# The three self-tests that are oracles
java -cp out pzformat.BuildingPlan      # 14,680 layouts
java -cp out pzformat.FootprintSnap
java -cp out pzformat.MaskRule          # fixed seeds 1 and 12345
```

**Noticed, out of scope:**
- `TreeScatter`/`TreePalette` (330 lines) are on the generation path but A2a is
  BLOCKED on A2-gate, and STATE §3 says `genMapSquare` deletes ~7,800 of their
  trees on load. **Resolve A2-gate before port step 7** — same argument as
  E15-before-F5, and it saves the same double work.
- The two repos' `STATE.md` and `CHUNKS.md` have diverged. `diff` them before
  folding these corrections in, or they land in one copy only.

**What the next chunk needs to know:**
- **F2 (`Json` + `GeoJson`, 182 lines) needs no PZ install and no media dir.**
  It is testable in complete isolation against `~/pzgis/tokyo/buildings.geojson`
  (59 features) and `~/pzgis/buildings.geojson` (7). Cheapest possible start.
- **Port the LCG before F3.** Six units draw from it; without it every later
  diff is noise and no oracle in Track F can clear.
- **F3 is `FootprintSnap` then `BuildingPlan`, and `BuildingPlan` is half the
  port** — 3,347 lines, zero dependencies, own self-test. Whoever wants a large
  self-contained chunk should take it.
- CHUNKS Track F needs amending: F4 drops `WaterTiles` and gains `TreePalette`,
  `GroundPalette`, `MaskRule`, `GroundMaterial`; F5 gains `GroundRegions`; F6
  gains `TreeScatter` and `BiomeMapWriter`.
